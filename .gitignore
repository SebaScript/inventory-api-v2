node_modules/

dist/
build/
*.tsbuildinfo

coverage/
.nyc_output/
junit.xml

.env
.env.*
!.env.example

# Filled-in Kubernetes config: it carries the database password
deploy/config.yaml
# Filled-in copies of the manifest templates: they carry the registry path of
# one specific AWS account. The committed *.example.yaml files are the source.
deploy/deployment.yaml
deploy/seed-job.yaml
deploy/prometheus.yaml
# Operational scripts written for one account, in Spanish. Same reason as the
# study material: useful locally, out of place in the repository.
deploy/*-aws.ps1

logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.pgdata/
.tmp/

.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

docs/
postman/

# Local working context for AI agents, never synced
CLAUDE.local.md

# Course material: rubrics and briefs live next to the repo, not in it
*.pdf
/rubrica.md
/Arquitectura_Multicloud_*.png

# Grafana Cloud push credentials, mounted into Prometheus
observability/grafana-cloud-token
# The filled-in scrape config: the template is prometheus.example.yml
observability/prometheus.yml
