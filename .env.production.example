COMPOSE_PROJECT_NAME=inventory-prod
NODE_ENV=production
API_PORT=3200
DB_USER=inventory_prod
DB_PASSWORD=change_me_production
DB_PORT=5434
DB_NAME=inventory_prod

# The image the pipeline published for this environment. The deploy job pulls
# this tag, so nothing is ever built on the server.
API_IMAGE=ghcr.io/sebascript/inventory-api-v2:prod
