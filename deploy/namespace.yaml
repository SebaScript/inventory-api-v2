apiVersion: v1
kind: Namespace
metadata:
  name: inventory
---
# The pod's AWS identity. EKS Pod Identity binds this account to an IAM role, so
# the container never carries a key or a secret of its own.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: inventory-api
  namespace: inventory
