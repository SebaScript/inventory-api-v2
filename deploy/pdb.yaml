# Voluntary evictions (node consolidation, upgrades) move one pod at a time,
# so there is always one replica serving.
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: inventory-api
  namespace: inventory
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: inventory-api
